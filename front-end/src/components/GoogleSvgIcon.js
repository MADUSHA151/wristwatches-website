import React from 'react';

function GoogleIcon(props) {
     return (
          <svg color="primary"  viewBox="0 0 24 24" fill="currentColor" {...props}>
               {"./assets/svg/google-icon.svg"}
          </svg>
     );
}

export default GoogleIcon;
